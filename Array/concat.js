// Concatenar dois ou mais arrays

const daughters = ['Maria', 'Bia']
const sons = ['Pedro', 'Tiago']

const all = daughters.concat(sons)

console.log(all)

console.log([].concat([1, 2], [3, 4], 5, [[6, 7]]))
