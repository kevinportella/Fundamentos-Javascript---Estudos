// Dados //

// criando uma variável para armazenar os dados //

var nome = "Caneta"
let quantidade = 10;  
var preco = 6.4;
let imposto = 1.5;
var precoFinal = preco + imposto;

console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(preco + imposto);
console.log(precoFinal);

// por ser uma variável, quer dizer q ela pode ser alterada //

nome = "Caneta BIC";
console.log(nome);

// foi definido a variável na linha 5 e foi alterada na linha 20 //

