// Código não executável !!!

// Procedural

process(value1, value2, value3)

// OO

object = {
    value1,
    value2,
    value3,
    process() {
        //...
    }
}

object.process() // o foco passou a ser o objeto

// Princípios importantes:

// 1. Abstração;
// 2. Encapsulamento;
// 3. Herança (prototype);
// 4. Polimorfismo;
